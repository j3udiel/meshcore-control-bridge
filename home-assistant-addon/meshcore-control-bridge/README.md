# Home Assistant Add-on Skeleton

This directory is a planning skeleton only. The add-on has not been tested and
is not published as a supported deployment mode.

Use the external Linux host or VM mode for the first real Companion test.

Before turning this into a working add-on, confirm the actual Home Assistant
installation type, serial-device access, persistent storage path, and a secure
way to provide the Home Assistant token.

