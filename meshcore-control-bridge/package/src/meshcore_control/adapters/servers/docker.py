from __future__ import annotations

from meshcore_control.adapters.servers.base import ServerProvider


class DockerProvider(ServerProvider):
    """Future Docker API or restricted socket proxy provider."""
